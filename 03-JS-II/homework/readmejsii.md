1. En un archivo de texto separado que debes crear, escribe explicaciones de los siguientes conceptos como si se lo estuvieras explicando a un niño de 12 años. Hacer esto te ayudará a descubrir rápidamente cualquier agujero en tu comprensión.

* `for`
* `&&`, `||`, `!`

// 'for': es un ciclo que permite repetir las instrucciones (pasos) un cierto numero de veces
// '&&': es un operador lógico que significa AND (como si se tratara de un 'y'), es el resultado verdadero si ambas expresiones son verdaderas también
   '||': es un oprador lógico que da el significado de OR, devuelve un valor verdadero si ambos operadores son verdaderos, de lo contrario va a devolver un falso
   '!' : también es un operador lógico que significa NOT, el resultado invierte la condicion de la expresion final, este operador actúa sobre un operando